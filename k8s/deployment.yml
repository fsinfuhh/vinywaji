---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: bitbots_drinks
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: web
      app.kubernetes.io/name: bitbots_drinks_server
  template:
    metadata:
      labels:
        app.kubernetes.io/component: web
        app.kubernetes.io/name: bitbots_drinks_server
    spec:
      containers:
        - name: server
          image: ghcr.io/bit-bots/bitbots_drinks:dev-latest
          imagePullPolicy: Always
          envFrom:
            - configMapRef:
                name: bitbots_drinks
                optional: true
            - secretRef:
                name: bitbots_drinks
                optional: true
          ports:
            - containerPort: 80
              name: http
          readinessProbe:
            tcpSocket:
              port: http
          livenessProbe:
            tcpSocket:
              port: http
